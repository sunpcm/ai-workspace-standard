# Existing Application

The primary router still points to a [missing document](missing.md) and does not
route to its declared architecture supplement.

Example placeholders such as `skills/your-skill/SKILL.md` and generic
`SKILL.md` references are not repository paths.

Generated artifact basenames such as `summary.md` and `batch_summary.md` are
not repository document references unless they are linked explicitly.
